name: PR
on:
  pull_request:
    branches:
      - develop
    types: [ opened, synchronize ]

jobs:
  terraform:
    name: Execute Terraform in deployment
    runs-on: ubuntu-latest
    env:
      aws-s3-bucket: ${{ secrets.AWS_S3_BUCKET }}
    strategy:
      matrix:
        directory: [ 'deploy/terraform/micro1', 'deploy/terraform/micro2', 'deploy/terraform/micro3' ]
    defaults:
      run:
        working-directory: ${{ matrix.directory }}
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v3

      - name: Setup Terraform
        id: setup-tf
        uses: hashicorp/setup-terraform@v2

      - name: Setup AWS Credentials
        id: setup-aws
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Terraform init
        id: init
        run: terraform init

      - name: Terraform format
        id: fmt
        run: terraform fmt -recursive

      - name: Terraform validate
        id: validate
        run: terraform validate

      - name: Terraform plan
        id: plan
        run: terraform plan

      - name: Terraform apply
        id: apply
        run: terraform apply -auto-approve

#      - name: Comment PR
#        uses: actions/github-script@v3
#        with:
#          github-token: ${{ secrets.GITHUB_TOKEN }}
#          script: |
#            const output = `#### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
#            #### Terraform Plan 📖\`${{ steps.plan.outputs.stdout }}\`
#
#            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;
#
#            github.issues.createComment({
#              issue_number: context.issue.number,
#              owner: context.repo.owner,
#              repo: context.repo.repo,
#              body: `#### Terraform checks in directory '${{ matrix.directory }}'\n` + output
#            })
